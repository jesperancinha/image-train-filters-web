version: "3.7"
services:
  itf-fe:
    hostname: frontend
    build:
      context: ./docker-files/.
    volumes:
      - ${PWD}/image-train-filters-fe/dist:/usr/share/nginx/html
    networks:
      - itf

  itf-backend:
    hostname: backend
    build:
      context: ./service/.
    networks:
      - itf

networks:
  itf:
